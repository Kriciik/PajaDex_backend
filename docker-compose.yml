services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: kriciik
      POSTGRES_PASSWORD: 123654
      POSTGRES_DB: pokemon_dev
    ports:
      - "5432:5432"
    volumes:
      - pg_pokemon_data:/var/lib/postgresql/data
    restart: always

volumes:
  pg_pokemon_data:
