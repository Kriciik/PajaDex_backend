// duplicate this file to frontend and export the type there as well

export type CleanCard = {
  id: string;
  name: string;
  image: string;
  setName: string;
  type: string[];
  category: string;
};
